import { YoutubeEmbedUrlPipe } from './youtube-embed-url.pipe';

describe('YoutubeEmbedUrlPipe', () => {
  it('should be defined', () => {
    expect(new YoutubeEmbedUrlPipe()).toBeDefined();
  });
});
